<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hometown Classifieds</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>

<body>
  <header class="display-flex justify-space-between align-center p-2">
    <img src= "/images/news.jpg" alt="newspaper" width="125" height="125">
    <h1>
      Hometown Classifieds
    </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
      <form action="/main_page">
          <input type="submit" value="Go Main Page" id="mainPageButton"/>
      </form>
      <form action="/profile">
          <input type="submit" value="Go Profile" id="profileButton"/>
      </form> 
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <a href="/login" id="loginButton">login</a>
        {{/if}}
      </nav>
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>
</body>

 {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

  
</html>
